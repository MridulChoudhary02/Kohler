{
  "name": "kohler-hospital-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/test_alert_order.js && node -e 'if (!process.env.FORCE_BUILD) { try { const p = require(\"child_process\").execSync(\"pgrep -f \\\"next dev\\\"\").toString().trim(); if (p) { console.error(\"\\x1b[31m[ERROR] Build blocked: `next dev` is currently running on PID(s): \" + p.split(\"\\n\").join(\", \") + \". Running `next build` concurrently risks cache corruption. Please stop the dev server first.\\x1b[0m\"); process.exit(1); } } catch(e) {} }'",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.3.3",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.424.0",
    "next": "14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tailwind-merge": "^3.7.0",
    "tailwindcss-animate": "^1.0.7"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.6.1",
    "postcss": "^8.5.28",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.5.4"
  }
}
